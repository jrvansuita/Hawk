module.exports = class Chart {

  constructor(title, label) {
    this.title = title;
    this.label = label;
    this.data = [];
    this.max = [];
  }


  addValue(val) {
    data.push(arr);
  }

  handleMax(val) {

    if (val instanceof Array) {
      val.forEach(function(item) {

      });
    }


    data.push(arr);
  }

};